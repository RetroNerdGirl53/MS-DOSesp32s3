# Name, Type, SubType, Offset, Size, Flags
nvs,      data, nvs,        ,    20K,
# otadata partition is not needed without OTA updates
# The app partition must start at an offset of 0x10000 (64K) or higher
factory,  app,  factory,    ,     3M,
# littlefs gets all remaining space
littlefs, data, spiffs,     ,     12500K,
